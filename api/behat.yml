imports:
  - ./src/Core/Infrastructure/Behat/Resources/config/suites.yaml

default:
#    formatters:
#      pretty:
#        verbose:  true
#        paths:    true
#        snippets: true

    extensions:
        FriendsOfBehat\SymfonyExtension:
          kernel:
            environment: test
            debug: true
        Behatch\Extension: ~
        Behat\MinkExtension:
          base_url: "https://localhost:8000/"
          default_session: symfony
          sessions:
            symfony:
              symfony: ~

