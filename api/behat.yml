imports:
  - ./src/Core/Infrastructure/Behat/Resources/config/suites.yaml

default:
    formatters:
      pretty:
        verbose:  true
        paths:    false
        snippets: false

    extensions:
        FriendsOfBehat\SymfonyExtension:
          kernel:
            environment: test
            debug: true
        Behatch\Extension: ~
        Behat\MinkExtension:
          sessions:
            symfony:
              symfony: ~
